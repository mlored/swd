package com.sd.isp.item

class ItemController {

    /*IItemService itemService

    def list(Integer max) {
        def items = itemService.getAll(PartB.class)

        if (request.getHeader('X-Requested-With')) {
            respond items, formats: ['json']
        } else {

            [itemInstanceList: items, itemsInstanceTotal: items?.size()]
        }
    }*/
}
